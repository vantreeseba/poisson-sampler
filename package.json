{
  "name": "poisson-disc-sampler",
  "version": "0.1.0",
  "description": "An implementation of poisson disc sampling",
  "main": "src/index.js",
  "scripts": {
    "test": "mocha --recursive -u exports --reporter=min",
    "test:watch": "mocha --watch --recursive -u exports --reporter=min"
  },
  "author": "vantreeseba@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.0.5"
  },
  "directories": {
    "test": "test"
  },
  "dependencies": {}
}
